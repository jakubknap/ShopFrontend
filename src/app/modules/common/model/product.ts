export interface Product {
    id: number,
    name: string,
    category: string,
    description: string,
    price: number,
    salePrice: number,
    currency: string,
    slug: string,
    image: string
}